name: Lint Forbidden Axios

on:
  pull_request:
  push:
    branches: [ main, master ]

jobs:
  check-forbidden-axios:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ripgrep
        run: sudo apt-get update && sudo apt-get install -y ripgrep

      - name: Check forbidden axios usage
        run: bash scripts/check_forbidden_axios.sh

